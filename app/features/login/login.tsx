
// calls the page with all the info about the login page
import LoginPage from "./login";

// renders the LoginPage component, which contains the form
export default function Page() {
  return (
    <main>
      <LoginPage />
    </main>
  );
}